{% extends 'base.html.twig' %}

{% block title %}LignesCommande index{% endblock %}

{% block body %}
    <h1>LignesCommande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantité</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lignes_commande in lignes_commandes %}
            <tr>
                <td>{{ lignes_commande.id }}</td>
                <td>{{ lignes_commande.Quantité }}</td>
                <td>
                    <a href="{{ path('app_lignes_commande_show', {'id': lignes_commande.id}) }}">show</a>
                    <a href="{{ path('app_lignes_commande_edit', {'id': lignes_commande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_lignes_commande_new') }}">Create new</a>
{% endblock %}
